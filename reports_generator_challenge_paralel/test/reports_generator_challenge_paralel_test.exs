defmodule ReportsGeneratorChallengeParalelTest do
  use ExUnit.Case
  doctest ReportsGeneratorChallengeParalel

  test "greets the world" do
    assert ReportsGeneratorChallengeParalel.hello() == :world
  end
end
